# frozen_string_literal: true

require_relative '../shared/load_deploy_config'

DeployConfig.load!

json_key = ENV['GOOGLE_PLAY_JSON_KEY'].to_s.strip
json_key_file(DeployConfig.secret_path(json_key)) unless json_key.empty?
package_name(ENV['GOOGLE_PLAY_PACKAGE_NAME'])
